import type { Settings } from './types'

export const defaultSettings: Settings = {
  theme: {
    mode: 'system',
  },
  layout: {
    contents: {
      clock: false,
      shortcuts: true,
    },
  },
}
